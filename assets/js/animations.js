document.addEventListener('DOMContentLoaded',()=>{const prefers=matchMedia('(prefers-reduced-motion: reduce)').matches;document.body.classList.add('page-ready');if(prefers)return;document.querySelectorAll('main > section, .card, .hero, footer').forEach((el,i)=>{el.classList.add('reveal');el.style.setProperty('--reveal-delay',Math.min(i*45,450)+'ms')});requestAnimationFrame(()=>document.querySelectorAll('.reveal').forEach(el=>el.classList.add('show')));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>io.observe(e))});